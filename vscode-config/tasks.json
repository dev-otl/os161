{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Copy Config",
            "type": "shell",
            "command": "cp src/kern/conf/DUMBVM src/kern/conf/${input:VersionName}",
            "problemMatcher": []
        },
        {
            "label": "Run Config",
            "type": "shell",
            "command": "cd src/kern/conf/;./config ${input:VersionName}",
            "problemMatcher": []
        },
        {
            "label": "Make Depend",
            "type": "shell",
            "command": "cd src/kern/compile/${input:VersionName};bmake depend",
            "problemMatcher": []
        },
        {
            "label": "Build and Install",
            "type": "shell",
            "command": "cd src/kern/compile/${input:VersionName};bmake;bmake install",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Run OS161",
            "type": "shell",
            "command": "cd root;sys161 -w kernel",
            "problemMatcher": []
        },
        {
            "label": "Run OS161 (no debug)",
            "type": "shell",
            "command": "cd root;sys161 kernel",
            "problemMatcher": []
        },
        {
            "label": "Build, Install and Run OS161 (no debug)",
            "type": "shell",
            "command": "cd src/kern/conf/ && ./config ${input:VersionName} && cd ../compile/${input:VersionName} && bmake depend && bmake && bmake install && cd ../../../../root && sys161 kernel",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "VersionName",
            "description": "Kernel version name"
        }
    ]
}